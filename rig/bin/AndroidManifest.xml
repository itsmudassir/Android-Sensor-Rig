<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rig"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="18" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.example.rig.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
         
		<service android:name="com.example.rig.SignalProvider"
		    android:enabled="true" android:label="SignalProvider"
		    >
		    <intent-filter >
		        <action android:name="com.example.rig.SignalProvider"/>
		        </intent-filter>
		</service>
        
        
         <service
            android:name="com.example.rig.ClientAidl"
            android:process=":client_aidl_service"
            android:exported="true"
            >
            <intent-filter >
                <action android:name="com.example.rig.ClientAidl"></action>
            </intent-filter>
        </service>
        
    </application>

</manifest>
